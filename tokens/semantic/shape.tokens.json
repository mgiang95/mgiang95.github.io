{
  "radius": {
    "control": {
      "$value": "{radius.none}",
      "$type": "dimension",
      "$description": "Corner radius for interactive controls (buttons, inputs). Square: poster geometry — rectangles, not pills."
    },
    "surface": {
      "$value": "{radius.none}",
      "$type": "dimension",
      "$description": "Corner radius for container surfaces (cards, panels). Square: poster geometry, flat color fields with sharp edges."
    }
  }
}
